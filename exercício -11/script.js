const Retangulo = {
  altura: 30,
  largura: 60,
  }

 
const calcularArea = Retangulo => {
const {altura, largura} = Retangulo
return altura * largura

}
console.log(calcularArea(Retangulo)) 

 /* function calcularArea(altura, largura) {
    let area = altura * largura
    console.log(area)
  }

  calcularArea(30, 60) */


  
  



